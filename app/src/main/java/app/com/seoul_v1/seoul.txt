    서울시 공공데이터(data.seoul.go.kr)에서 제공하는
    서울시 동물병원 정보를 추출해서
    LogCat 화면에 출력하는 코드를 작성
    추가해서 List를 만들어서 List에 데이터를 추가하는 부분까지
    List를 이용해서 RecyclerView에 데이터를 보일 수 있다.

    1. 서울공공데이터 요청 url 문자열 생성
    => naver와 차이점 : 내가 필요한 정보를 검색하기가 안된다.
    예) 서울펫 이라는 병원을 찾고 싶다.(x)

    2. URL 객체 생성
    3. URL 객체를 이용해서 연결시도(connection 객체 생성)
    4. 연결 정보를 취득
    5. 연결 정보를 통해서 query 전송

    4와 5는 동시에 이루어 진다.

    6. inputStream으로 결과를 받고,
    7. bufferedReader를 사용해서 문자열을 추출

    8. 문자열을 JSON 객체로 변환하고
    9. JSON 객체로 부터 각 항목을 추출해서
    10. LIST에 담는다.
